# Recent changes

Newest first. Keep the **newest 15** entries here; `python tools/archive_changes.py` moves the rest to
`docs/archive/CHANGES-YYYY-MM.md`. Stable facts belong in `ARCHITECTURE.md`, not here.

Entry format (heading must be exactly `## <version> — <YYYY-MM-DD> — <title>` so the script can parse it):

```
## 0.2.1 — 2026-09-24 — Short title
- **What:** user-visible change in one or two lines.
- **Where:** files / functions touched.
- **Notes:** invariants, gotchas, decisions, things deliberately NOT done — what the next agent must know.
```

Add new entries directly below this line.

## 0.15.0 — 2026-09-24 — Reveal in File System (multi-select) and Find in Folder
- **What:** "Locate" is now "Reveal in File System" and works on the whole selection: one Explorer/Finder window per folder, with all of that folder's selected files selected. The new "Find in Folder" command jumps the library grid to where the selected files live and selects them there. Both commands are in the grid menu, in the new board right-click menu and in the palette. The inspector button now reads "Reveal".
- **Where:**
  - `server.py`: `reveal_paths()` + `_win_select_in_folder()` (ctypes → `SHOpenFolderAndSelectItems`), new `POST /api/assets/reveal {ids}` → `{windows}`. The old per-asset `/api/assets/{aid}/reveal` goes through the same code.
  - `canvas.js`: `onContextMenu` option; a right-button press that never moves 4px (`zoomdrag.moved`) opens it instead of zooming; `selectedAssetIds()` in the returned API.
  - `index.html`: `showCanvasCtx`, `revealAssets`, `findInFolder`, `expandTreeTo`, `commonDir`/`parentDir`/`pathParts`, `waitForItems`, `scrollGridToIndex`, `activeSelectionAssetIds`.
- **Notes:**
  - `explorer /select,` only takes ONE file, which is why the old Locate selected just the first. `SHOpenFolderAndSelectItems` reuses an Explorer window already open on that folder. It needs COM in the calling thread (`CoInitializeEx`, the endpoint runs `reveal_paths` in `asyncio.to_thread`). If it fails, the code falls back to `explorer /select,` on the first file. On macOS the Finder `reveal {list}` is used; on Linux only the folder opens (no portable way to select). Files missing on disk are skipped; their folder still opens if it exists.
  - Find in Folder view choice: one source → `commonDir` of the parent folders. It becomes the folder filter, which is recursive, or the source filter if it is the source root. Several sources → All. Search (`#q`) and the kind filter are cleared so nothing hides the files. `expandTreeTo` returns the tree's own spelling of the folder, because the row's "on" check compares it to `state.filter.folder` exactly.
  - The grid pages in 400 at a time, so `waitForItems` polls until every target is loaded (10 s cap) before selecting and scrolling. If the board is shown full-width, the split is switched on so the grid can be seen.
  - Excluded files can't be shown and get an alert. Missing files still appear, because the folder views include them.
  - Checked with Playwright on an isolated `SIGHT_HOME`: one folder, one folder with two files, two subfolders, source root, two sources, right-drag still zooms, and no JS errors. Multi-file Explorer selection was checked through `Shell.Application` `SelectedItems()`.

## 0.14.1 — 2026-09-24 — Library: deleting a folder marks the files inside it missing
- **What:** Files inside a folder that is sent to the Recycle Bin, or moved out of a watched source, now turn "missing" right away. Before, they stayed "ready" until the next rescan or restart. It looked like a text-file bug only because the reported case was a folder holding only `.log`/`.ini` files (Unreal crash folders).
- **Where:** `library.py`: new `Library._mark_gone(fp)`, used by the watchdog handler's `on_deleted` and by the `on_moved` fallback (files only).
- **Notes:**
  - On Windows, watchdog reports a folder recycle or a move out of the tree as ONE `deleted` event for the folder, with `is_directory=False` (the path can no longer be stat'ed). So every deleted path is treated as a possible folder: `_mark_gone` matches the path itself plus `substr(path,1,len(prefix)) = prefix + os.sep`. It uses `substr`, not `LIKE`, so `%`/`_` in names don't need escaping. `shutil.rmtree`-style deletes already sent one event per file. Renaming a folder inside the tree sends per-file `moved` events and already worked.
  - `excluded` rows are not flipped to missing, same as `scan_source`. The path match is case-sensitive, same as everywhere else.
  - Missing entries are still never removed automatically. Only a manual purge removes them (the 🧹 button on the sidebar's Missing row, the grid context menu, or the palette → `/api/missing/purge`). Scans only set the status, and `_reconcile_moved` carries metadata over to moved files.

## 0.14.0 — 2026-09-24 — Canvas: swap thumbnails for originals when zoomed in
- **What:** Board items still start on the 360px library thumbnail, but a visible 2D picture that is shown bigger than its thumbnail now loads the original in the background and swaps it in; zooming back out returns to the thumbnail.
- **Where:** `canvas.js`: `FULL_RES_EXT` / `fullResUrl` (module level), `scheduleResCheck` / `syncResolution` / `shownImageWidth` / `loadFull` in `createCanvasView`; `buildItemEl` stores `img.dataset.thumb/full/fullW/res` and its `load` listener is no longer `once` (it re-runs pixelation and records `thumbW` on every swap); hooks in `worldTransform` and `layoutItemEl`.
- **Notes:**
  - Thresholds: upgrade above 1.25× the thumbnail width (in device pixels), downgrade below 0.8×; the gap is deliberate hysteresis. Upgrades only for items in view, nearest the centre first, max 3 concurrent; the original is `decode()`d off-screen before the swap, so there is no blank frame.
  - Formats: JPEG/PNG/GIF/WebP/BMP/AVIF via `/api/file`, PSD via its composite. SVG deliberately excluded (no dependable `naturalWidth`, which crop and pixelation read). TIFF/HEIC/JXL are not browser-decodable and stay on the thumbnail; a server-side converted preview would be the way to add them. Animated GIFs will animate once swapped.
  - A failed load marks the `<img>` `res=failed` and it stays on the thumbnail for the session. Not tested in a browser (no Playwright in the runtime).

## 0.13.0 — 2026-09-24 — Canvas: resize handle follows the cursor's corner
- **What:** The transform frame's single resize handle is no longer fixed at the bottom-right. It jumps to the frame corner in the cursor's quadrant, and dragging it pins the opposite corner. Works for one item (rotated too) and for a group.
- **Where:** `canvas.js`: CSS `.cframe[data-corner=…] .chandle`, `updateHandleCorner` / `frameGeom` / `CORNER_SIGNS` (called from `updateSelectionFrame` and the window `pointermove` when no drag is under way), `resizeAnchor`, `startResize` (`sx`/`sy`/`anchor`, group `ax`/`ay`), the `resize` / `gresize` branches of `onPointerMove`, `uncropForResize` (re-pins the anchor).
- **Notes:**
  - Still one handle element, positioned via `frameEl.dataset.corner`; the corner is frozen while `drag` is set. The cursor style follows the rotated diagonal.
  - Single-item resize now measures pointer movement along the item's own (rotated) axes and keeps the opposite corner in place; before it used world deltas and kept `x/y`, which drifted on rotated items. Ctrl-snap snaps the dragged corner in world axes (exact only for unrotated items, as before).
  - Group resize is still uniform, anchored at the opposite corner of the union bounding box. No edge (side) handles were added.

## 0.12.1 — 2026-09-24 — 3D: explode guards for models with huge part counts
- **What:** Explode can no longer freeze the page on a model split into thousands of tiny meshes (e.g. one mesh per face). Up to 1000 parts per assembly it uses the explosion graph. Bigger assemblies, and any assembly reached after a 400 ms budget, use a simple axial mode: each part moves along the dominant axis of its offset from the assembly center. Above 50,000 meshes in total the explode control is hidden.
- **Where:** `index.html`: `EXPLODE_GRAPH_MAX` / `EXPLODE_MAX_MESHES` / `EXPLODE_BUDGET_MS`, `buildExplodeRig` (`solveAxial`, one post-order pass for "has a mesh", BFS queue instead of recursion, `place` / `pushAll` / `largest` helpers), `applyExplode` (iterative over `rig.list`, reusing a per-item `off` vector).
- **Notes:**
  - Measured: 800 touching parts (graph) 134 ms, 3150 (axial) 21 ms, 60,000 rejected in 4 ms.
  - Nothing may spread a per-part array into arguments (`Math.max(...x)`, `push(...x)` throw a RangeError at ~100k elements) or recurse per part (ride-along chains can be thousands long). `rig.list` is the removal order reversed, which puts every carry before the parts that ride on it.
  - The budget is checked between assemblies, so one graph solve (≤1000 parts, ~250 ms worst case) can overrun it once.

## 0.12.0 — 2026-09-24 — 3D: assembly-aware explode (explosion graph, grounded part, one axis per part)
- **What:** Explode no longer pushes every top-level child radially away from the center. It follows the explosion-graph method of Li, Agrawala, Curless & Salesin (SIGGRAPH 2008), which is also how CAD auto-explode behaves: the largest part of each assembly stays put, every other part slides out along one model axis (±X/±Y/±Z) in an unblocked direction, parts ride along with the part they rest on, and sub-assemblies from the file's node hierarchy move as a unit and then explode inside themselves. The slider is staged: top-level parts first, then their insides, each part easing in turn.
- **Where:** `index.html`: `buildExplodeRig(THREE, obj)` (replaces the radial `explodeMap` build in boot3d), `applyExplode(rig, t)` (signature changed, `explodeScale` removed).
- **Notes:**
  - Solved once per load, with `obj` temporarily at identity so the model's own axes are the canonical directions; offsets are stored in each part's parent-local space, so the 90° turns never invalidate them. "Up" (the preferred direction on ties, down is least preferred) is the model axis that maps to world up at load time.
  - Assembly = a node with 2+ children that contain meshes (single-child wrappers are skipped). A Mesh is always atomic. Flat files (OBJ, most STL-to-glTF) get one level.
  - Blocking and contact use precise **bounding boxes**, not geometry (the paper uses real geometry and splits containers — not done). A part nested inside another's box is blocked on all sides; the fallback then removes the part with the fewest blockers. Expect odd but valid results for concentric parts and large voxel-like grids of equal parts (long ride-along chains).
  - Cost is O(n²·6) for the pair tests; escape distances are cached and invalidated for the neighbours of each removed part. 800 touching parts solve in ~140 ms.
  - Explode still moves parts by `position` only; `fitRig` sets t = 0 to measure the floor.

## 0.11.0 — 2026-09-24 — 3D: hide/isolate meshes, F keeps the view direction, Ctrl+right-drag explode, Solid material and FOV settings
- **What:** In the 3D lightbox: H hides the selected meshes, Ctrl+H hides every other mesh, Alt+H shows all. F now frames the selection (or the whole model as it is right now: explode and hidden meshes count) without turning the camera; it only moves and zooms. Explode moved from Ctrl+wheel to Ctrl+right-drag (right or up = further apart). Gear panel: new "Solid shading" section (Color, Metallic, Roughness for the Solid mode) and a Camera "Field of view" slider (10–120°).
- **Where:** `index.html`: boot3d Ctrl+RMB `pointerdown/move/up` (before OrbitControls; replaces the 0.10.0 wheel handler), inspector `boxOf` / `selBox` / `modelBox` / `hide(mode)`, F handler (`frameBox` for meshes, `frame()` only for splats), H / Ctrl+H / Alt+H in the main keydown handler, `.li-item.hid` (dimmed in the Meshes list), `DEFAULT_V3D` (`solidColor`, `solidMetal`, `solidRough`, `camFov`), `applyBaseMaterial` (tags Solid materials with `userData._v3dSolid`), syncRig retints them live (`shadeApplied.solid` key), `makeCameraRig().step()` applies `camFov` every frame, `v3dCameraRows` / `renderV3DPanel`.
- **Notes:**
  - Hiding uses `mesh.visible`, so shadows, Ctrl+click picking (visible meshes only) and the wire overlay (a child of the mesh) follow. It is whole meshes only: a material selected on a multi-material mesh hides the entire mesh. H also clears the selection; Ctrl+H keeps it. Hidden state is not persisted and resets when the model is opened again.
  - F no longer returns to the opening camera for meshes (`home` is still used by splats and by turns). `selBox` and `modelBox` measure visible meshes, falling back to all of them when everything is hidden.
  - The Ctrl+RMB handler swallows the gesture even on a single-mesh model (no explode), so it never dollies.
  - FOV is applied in `step()` because both the mesh viewer and the splat viewer call it every frame. The opening framing does not depend on FOV; F refits with the current FOV.

## 0.10.0 — 2026-09-24 — 3D: Ctrl+wheel explode, F frames the selection, floor/grid/shadow/anchor on the toolbar
- **What:** In the 3D lightbox, Ctrl+wheel changes the explode amount (5% per notch, toast shows the value). F frames the selected mesh/material/texture when there is a selection, the whole model otherwise. Floor, Grid, Shadow-only and the light anchor (World ↔ Camera, the icon switches globe ↔ camera) moved from the gear panel to icon buttons in the bottom toolbar, next to the 90° turn button; the panel's Floor section keeps only Shadow strength.
- **Where:** `index.html`: boot3d `wheel` handler on `renderer.domElement` (bound before OrbitControls), `initThinSlider` now returns `{ set }` (kept as `light.explodeSlider`), `makeCameraRig().frameBox(box, label)`, inspector `selBox()`, F handler, `V3D_ICONS` + `paintShadeButtons` (anchor icon/title), `toggleShadeFlag` (floor ↔ shadowOnly coupling), toolbar markup, `renderV3DPanel` (rows removed), hint strings.
- **Notes:**
  - Ctrl+wheel must stay bound before `new OrbitControls` and call `stopImmediatePropagation()`, or the wheel also zooms; `preventDefault()` stops the browser's Ctrl+wheel page zoom. On a single-mesh model (no explode) Ctrl+wheel does nothing (it is still swallowed, no zoom).
  - Floor, Grid and Shadow-only reuse the `data-toggle` mechanism of the wireframe / x-ray buttons (`toggleShadeFlag` + `paintShadeButtons`). Shadow-only is a mode of the floor: turning it on turns the floor on; turning the floor off clears it.
  - `frameBox` keeps the current viewing direction and fits the selection's bounding sphere; it does not change `home`, so F with nothing selected still returns to the opening framing.

## 0.9.0 — 2026-09-24 — Ctrl+click picks a mesh in the 3D view; dropdowns stay inside the window
- **What:** In the 3D lightbox, Ctrl+click on the model selects the mesh under the cursor (orange highlight, marked in the info panel's Meshes list, UV pane follows); Ctrl+click it again deselects; a click on empty space clears any selection (mesh, material or texture). Every `<select>` in the app now opens an in-page list that stays inside the window (flips above the control near the bottom edge) — the UV texture-channel picker used to open its native list below the window.
- **Where:** `index.html`: boot3d pick handlers on `renderer.domElement` (after OrbitControls setup), new `setInspectSel(sel, reveal)` (shared with `onLightInfoClick`), "Select dropdowns" block (`#selPop`, `openSelectPop`, `closeSelectPop`, capture-phase `mousedown`), Escape in the main keydown handler, `.sel-pop` CSS.
- **Notes:**
  - A pick is a left click that moved ≤ 4 px (a drag is an orbit / Ctrl-drag pan). Alt is excluded (Alt-drag rotates the lights). Only meshes in `v3dInspect.meshes` are raycast, so highlight overlays, floor and gizmos never get picked. A plain click on the model deliberately does nothing.
  - A viewport pick opens the Meshes section of the info panel (if the panel is shown) and scrolls the entry into view; with the panel hidden the highlight still shows.
  - `#selPop` follows the `#colorPop` pattern: the `<select>` stays the source of truth (value set, then `input` + `change` fired), so no existing handler changed. Keyboard use of a focused select is left native. `multiple` / `size > 1` selects are not intercepted. The popup closes on outside pointerdown, Escape, resize, page scroll, and via `registerAutoClose`.

## 0.8.0 — 2026-09-24 — Scan ignore list; compiled .obj files skipped; 3D textured mode no longer freezes on multi-material meshes
- **What:** Settings → Scanning → Ignore: one pattern per line (`*.meta`, `Library/`, `BurstCache/`, `Library/PackageCache`, `# comment`), *Apply* hides matching files and folders in every source and rescans. `.obj` files that are compiler output (MSVC/COFF — Unity BurstCache, VS `obj/`) are no longer indexed as 3D models; ones indexed earlier disappear on the next scan. Fix: the 3D lightbox froze (render loop threw every frame) in Textured mode on FBX files with a multi-material mesh.
- **Where:** `library.py`: `IgnoreRules`, `rel_parts`, `Library.ignore` (loaded in `__init__` from the new `meta` table, key `scan_ignore`), `hidden()`, `set_ignore()`, `scan_source` (dir pruning + `hide` list), `set_excluded` (re-including a folder does not revive ignored files), `_touch_one` (watcher). `fileinfo.py`: `obj_is_binary` (shared by scanner and info overlay). `server.py`: `PUT /api/scan/ignore`, `/api/state` → `scanIgnore`. `index.html`: Settings "Scanning" block (`#scanIgnore`, `applyScanIgnore`, `refreshMeta` fills the textarea unless it has unsaved edits), `applyXray` / new `xrayMaterial`.
- **Notes:**
  - Ignored files reuse `status='excluded'` (same as an excluded folder), so tags/ratings/notes survive and every query that already skips excluded rows works unchanged. Consequence: revival must check both — `set_ignore` revives an excluded row only if it is in no excluded folder and not `hidden()`; `set_excluded` skips `hidden()` rows when re-including. Anything new that flips rows back to 'ready' must do the same.
  - Patterns are relative to the source root (the root folder's own name never matches) and case-insensitive; no `!` negation. The list is global, not per source. It lives in the library DB, not settings.json, because the scanner needs it at startup before any page is open.
  - Freeze cause: `c.material` is an array for multi-material meshes; Textured mode restores that array from `origMats`, and `applyXray` (run after every base-mode switch to reapply x-ray) read `m.userData` of the array. Other shading modes replace the array with one material, so only Textured broke. Anything else that walks `c.material` must handle arrays.
  - Tested: API round trip on an isolated `SIGHT_HOME` (ignore, clear → restored, a pre-existing 'ready' COFF row hidden by rescan, watcher skips new ignored/COFF files), Settings UI with Playwright, and the reported Unity crate FBX: solid → x-ray → textured → x-ray with no page errors.

## 0.7.0 — 2026-09-24 — UV pane toolbar: UV set and texture channel; static highlight; UV button on the left
- **What:** the UV pane has a bottom toolbar (the 3D shading bar's look): UV1/UV2/… buttons when the shown meshes carry more than one UV set, and a channel list (RGB, RGBA over a checker, R, G, B, A as grayscale) when a texture is under the UVs. The polygon highlight no longer pulses (static orange). The *UV* button moved to the left end of the 3D toolbar (the pane opens on the left), and with the split on that toolbar centres on the model's half.
- **Where:** `index.html`: `refreshUv` (builds `.uv-bar`, `uvChan` / `sight.uvChan`, `UV_CHANNELS`), `makeModelInspector` → `renderUV(sel, chan)` (`uvCount`, `uvGeometry`, `bgShader`, returns `sets/set/hasBg`), `ins.uvSet` (per model, not persisted), `select()` (no `onBeforeRender`), `#v3dShade` markup and `#v3dShade { left: … var(--uv-left) }`; `.opt select` rule shared with `.uv-bar select`.
- **Notes:** UV sets are `uv`, `uv1`, `uv2`, `uv3` (three's names for TEXCOORD_0..3). The chosen set is fed to the UV shader through a per-(mesh, set) proxy `BufferGeometry` that shares the mesh's attribute objects as `position` + `uvSel`; these proxies are cached and deliberately never disposed — `geometry.dispose()` frees the GL buffers of shared attributes, which the model still renders with. Channels: sRGB textures sample as linear and the readback re-encodes, so R/G/B show stored values; alpha is pre-decoded in the shader so it survives the encode. Normal/ORM maps (non-sRGB) are shown raw. Meshes lacking the chosen set are skipped. Tested with Playwright on an isolated `SIGHT_HOME` with a generated GLB (TEXCOORD_0 + TEXCOORD_1, RGBA PNG): set switching, A/RGBA/R views, toolbar order, no console errors. No library model has a second UV set, so real-world multi-set files were not tried.

## 0.6.0 — 2026-09-24 — 3D: correct material count, collapsible Materials/Textures/Meshes, UV editor split (U)
- **What:** a model whose meshes share one material now shows 1 material (was one per mesh). The info overlay's Materials / Textures / Meshes are collapsible (click the header, closed by default, remembered), each list scrolls on its own; a new Meshes list (name, triangles, materials) highlights a mesh on click. `U` or the new *UV* toolbar button splits the 3D lightbox: UV pane | divider | model. The UV pane shows the selection's UVs (orange; all UVs in white with nothing selected) over its base-color texture and pans/zooms like an image; `F` and 1–9 go to the pane under the cursor. While the split is on, the *i* button and the info panel move to the model's side.
- **Where:** `index.html`: `cloneModel` (one clone per material), `attachPanScroll` (nested), new `bindSplitDrag` (the library/board divider now uses it too), `lightPane` in the `pointerover` hover router, `attachViewer(…, opts)` (`embedded`, returns `{fit, zoom}`), `#v3dShade [data-act=uv]`, keydown (KeyF / digits / KeyU in the 3D block), `renderLightInfo` / `inspectSection` / `onLightInfoClick`, `toggleUV` / `syncUvPane` / `refreshUv`, `makeModelInspector` (`matEntry` merge, `meshes`, `targets`, `renderUV`), CSS `.light-info .sec`, `.li-list`, `.uv-pane`, `.light-frame.uv-on`, `--uv-left`.
- **Notes:**
  - Cause of the wrong count: `cloneModel` (every open of a cached model, and the first open too) cloned the material per mesh, turning one shared material into N instances (also N shader programs). Fixed at the source; the inspector additionally merges instances with the same `type|name` (FBX/OBJ loaders can create per-mesh copies).
  - UV picture: rendered by the viewer's renderer into a W×H render target (texture size, 2048–4096 on the long side), geometry drawn with `gl_Position = uv` (ShaderMaterial; three declares `uv` for it), fill + wireframe, multi-material meshes limited to the selected groups; read back, sRGB-encoded, `toBlob` → object URL (revoked on replace/close). glTF: v = 0 at the image top (texture `flipY` false); with a texture its `flipY` decides. Only the first UV set is drawn. A 4096² render stalls the main thread for a moment (readback + pixel loop); it is redrawn on every selection change while the split is on.
  - UV mode (`sight.uvMode`) and the split ratio (`sight.uvSplit`) persist; the pane is removed on every asset switch (`showInLight` → `syncUvPane`) and re-added by `boot3d` once the inspector exists, so images/splats never get it. Info section state: `sight.infoOpen` `{mat, tex, mesh}`.
  - Tested with Playwright on an isolated `SIGHT_HOME`: 30 meshes / 1 material GLB (now "1 material · 30 meshes"), 1-mesh / 3-texture GLB, UV toggle, divider drag, 2 → 200% and F → Frame on the UV pane under the cursor, no console errors.

## 0.5.0 — 2026-09-24 — Info overlay: button top-left, richer video, 3D materials/textures with highlight
- **What:** the *i* button moved from the lightbox bar to a round button in the stage's top-left corner; the panel opens beside it. Video: duration now shows the exact frame count, plus color profile (Rec. 709 / Rec. 2020 / Rec. 601 / P3, transfer PQ/HLG), color range, SDR/HDR, chroma subsampling, scan, start timecode; key fields (duration, fps, size, codec) come first. 3D meshes: "Materials" (rendered shaded balls, name, triangles, texture count) and "Textures" (thumbnail, name, resolution, slots such as "Roughness, Metalness"); clicking one highlights its polygons in pulsing orange with a faint wireframe, clicking again clears it. A failed info request is shown ("restart Sight to load the new server code" on a 404) instead of silently showing nothing.
- **Where:** `fileinfo.py`: `_color_profile`, `_count_frames`, `_video_rows` (reordered), `_av`; `index.html`: `.info-fab` / `.li-*` CSS, `renderLightInfo` (errors, scroll kept, inspector sections), `onLightInfoClick`, new `makeModelInspector` (replaces `modelSceneInfo`), `v3dInspect` global next to `v3dCam`, set in `boot3d`, disposed in `dispose3d`.
- **Notes:**
  - Frame count: `ffmpeg -i f -map 0:v:0 -c copy -f null -` (remux, no decode — ~0.1 s for small files, disk speed for big ones), last `frame=` in stderr; skipped over 6 GB or after 15 s, then "≈ fps × duration" is shown. Color tags come from the pixel-format parens of the `ffmpeg -i` banner: "tv, bt709" = all three agree, otherwise "matrix/primaries/transfer".
  - Thumbnails are rendered by the viewer's own renderer into a 72² MSAA render target and read back, one per frame (a material in the ball scene's light setup compiles a new program). Render targets are linear with no tone mapping in three r185, so sRGB is encoded by hand with a LUT; non-sRGB textures (normal/ORM) are shown raw. glTF textures (`flipY=false`) are drawn on a Y-flipped quad. Materials with vertex colors are cloned without them for the ball. Up to 160 thumbnails.
  - Highlight overlays live in the **scene**, not in the model: `applyBaseMaterial` / x-ray / wireframe traverse the model and would restyle or crash on them. Each overlay shares the source geometry, overrides `updateMatrixWorld` to copy the source's world matrix (and `bindMatrixInverse` for skinned meshes, bound to the same skeleton), so explode and turns carry it along. Multi-material meshes: the other groups get an invisible material. Highlight color is fixed orange (the theme accent can be near-white).
  - Material/texture lists come from `origMats`, so the current shading mode (solid/normal) does not change them; the Scene counts use them too (the old code counted the swapped solid material).
  - Closing the overlay clears the highlight. Splats keep only the Scene counts. Tested with Playwright on an isolated `SIGHT_HOME` (18-material GLB, 1-material/3-texture GLB, H.264 mov).

## 0.4.0 — 2026-09-24 — Lightbox file info overlay (I)
- **What:** an *i* button in the lightbox bar (and `I`) toggles a translucent Blender-style info overlay, top-left. It lists general facts (name, type, size, dimensions for images/PSD, modified, added, folder) and then sections that only exist for that type: image format/color/bit depth/ICC/DPI/JPEG subsampling/animation frames + EXIF camera block + embedded PNG text (e.g. SD `parameters`); video codec/profile/frame size/fps/pixel format/bit depth/HDR/bitrates + audio + subtitle tracks + tags; audio codec/sample rate/channels/bit depth (lossless only)/cover art/tags; font family/style/version/weight/glyphs/UPM/outline type/variable axes/color tables + credits; PDF pages/page size/outline/encryption + metadata; text encoding/line endings/lines/words, CSV columns, JSON validity/keys, Markdown headings; PSD mode/depth/layers; .blend version; .sketch pages; .ai as PDF; 3D file header counts (glTF nodes/meshes/materials/extensions, OBJ v/vt/vn/f, STL triangles, PLY elements + splat SH degree, FBX version, USDZ contents, .splat/.spz counts) plus a live "Scene" section from the viewer (meshes, vertices, triangles, materials, textures + largest size, skins, morphs, animations, bounds; splat count for splats).
- **Where:** new `app/fileinfo.py` (`file_info()` dispatch, one reader per type); `library.py` `Library.file_info()` (in-memory cache keyed by mtime+size); `server.py` `GET /api/assets/{aid}/info` → `{sections:[{title, rows:[[label, value]]}]}`; `index.html`: `#infoBtn`, `#lightInfo`, `.light-info` CSS, `toggleLightInfo` / `renderLightInfo` / `modelSceneInfo` / `setSceneInfo` (next to the PSD panel code), `showInLight` resets `light.sceneInfo`, `boot3d` calls `setSceneInfo` for meshes and splats, keydown `hotkey(e, "KeyI")`.
- **Notes:** open state is the `sight.lightInfo` setting ("1"/"0"), so it persists and follows the user across assets; the server is only asked while the overlay is visible, and the client caches per `id:mtime:size`. Video/audio details come from parsing the `ffmpeg -i` banner (no ffprobe ships with imageio-ffmpeg) — `_parse_ff` / `_split_top`; if ffmpeg changes its banner format this is where it breaks. Only existing dependencies are used (Pillow, pymupdf, psd_tools); fonts are read from the sfnt tables directly (TTF/OTF/WOFF1), WOFF2 falls back to Pillow's family/style only. `.obj` files that are MSVC COFF objects (Unity BurstCache etc.) are reported as such instead of "0 vertices". A reader that throws yields a single "Error" row, never a 500. Asset `width/height` is the thumbnail size for non-image kinds, so the general "Dimensions" row is shown only for images and PSDs. Not done: EPUB page count (needs a full layout), HEIC depth maps, per-stream details beyond the first video/audio stream. Tested with Playwright on an isolated `SIGHT_HOME` (jpeg with EXIF, mov, mp3, otf, pdf, glb, psd, stl, json), no console errors; Shift+I does not toggle.

## 0.3.1 — 2026-09-24 — 3D viewer: thumbnail stays up until the model is drawn; no progress bar for cached models
- **What:** opening a mid-size or large model no longer flashes an empty grey viewport. The thumbnail backdrop now stays until the model has actually been drawn, then fades out. A model that is still in the in-memory model cache gets the same thumbnail backdrop but no "Loading model…" bar (there is nothing to download).
- **Where:** `index.html`: `boot3d` (`cached` flag from `modelCache.has(modelCacheKey(aid))` only drops the plate; `endLoading()` moved from right after `loadModelCached` to the end of the mesh setup, after a double rAF); new `modelCacheKey()` shared with `loadModelCached`.
- **Notes:** cause of the grey screen: the cover used to lift as soon as the model object existed, but the first frame with it (shader compile + texture upload) blocks the main thread, so the fade revealed an empty canvas for that long. The render loop's rAF is queued before ours, so it draws the model in the first of the two frames, under the cover. A short-lived 0.2.9 dropped the backdrop entirely for cached models; that made the grey screen worse and was reverted. `renderer.compileAsync()` was tried and rejected: for a 131 MB GLB it never resolved (the live loop replaces material programs while it polls the old ones). Models larger than Settings → RAM cache are never cached, so they always show the plate. Splats keep their own path (no cache, `endLoading()` after `mesh.initialized`). Checked with Playwright on an isolated `SIGHT_HOME` (185 KB and 131 MB GLB, first vs second open, screenshot when the cover goes).
